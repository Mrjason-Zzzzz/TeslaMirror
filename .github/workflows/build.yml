name: Android CI/CD Automation Build Pipeline

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout Repository Source Code
      uses: actions/checkout@v4  # 顺带升级到最新的 v4 基础迁出依赖

    - name: Set up JDK 17 Runtime Environment
      uses: actions/setup-java@v4  # 升级到 v4 环境依赖
      with:
        java-version: '17'        # 升级到 Java 17，新版 Android 编译插件更稳定
        distribution: 'temurin'
        cache: gradle

    - name: Grant Execute Permission for Gradle Wrapper
      run: chmod +x gradlew

    - name: Trigger Production APK Compilation (Assemble Release)
      run: ./gradlew assembleRelease

    # ========================== 核心修复点 ==========================
    - name: Upload Compiled Production APK Package Artifacts
      uses: actions/upload-artifact@v4  # 🔥 彻底升级到官方要求的 v4 版本！
      with:
        name: TeslaCast-Core-Engine
        path: app/build/outputs/apk/release/app-release-unsigned.apk
    # ===============================================================
